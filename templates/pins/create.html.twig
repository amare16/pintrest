{% extends 'base.html.twig' %}

{% block title 'New Pin' %}

{% block body %}
    <h1>Create Pin</h1>

    {{ form_start(myForm) }}
        {{ form_widget(myForm) }}

        <input type="submit" value="Create Pin" formnovalidate>
    {{ form_end(myForm) }}
{% endblock %}